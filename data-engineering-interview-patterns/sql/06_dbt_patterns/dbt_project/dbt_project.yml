name: interview_patterns_demo
version: '1.0.0'
config-version: 2

profile: interview_patterns

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

models:
  interview_patterns_demo:
    staging:
      +materialized: view
    intermediate:
      +materialized: table
    marts:
      +materialized: table
